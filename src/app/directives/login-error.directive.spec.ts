import { LoginErrorDirective } from './login-error.directive';

describe('LoginErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginErrorDirective();
    expect(directive).toBeTruthy();
  });
});
